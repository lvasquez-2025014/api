services:
  - type: web
    name: keyauth-api
    env: node
    rootDir: backend
    buildCommand: pnpm install && pnpm build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
